# Minigame Arcade Pygbag configuration
#
# Keep this file minimal until the default browser build has been
# successfully validated. Add overrides only when a demonstrated
# packaging problem requires one.

[DEPENDENCIES]
ignoreDirs = ["/.git", "/.venv", "/venv", "/__pycache__", "/.pytest_cache", "/build", "/dist"]
ignoreFiles = [".gitignore", "requirements-dev.txt"]